export interface Employee{
    emp_id ? : any;
    emp_name ? : any;
    emp_mob ? : any;
    emp_email : any;
    emp_adhar_number ? : any;
    emp_pan_number ? : any;
    emp_dob ? : any;
    emp_gender ? : string;
    emp_marital_status ? : string;
    emp_location ? : any;
    emp_password ? : any;
    is_expandable? : boolean;
}